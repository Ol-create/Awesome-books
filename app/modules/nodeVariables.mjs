// Navigation
const navlist = document.querySelectorAll('.link');
const section = document.querySelectorAll('.section');
const bookShelf = document.getElementById('bookList');
const newBook = document.getElementById('add');
const book = newBook.querySelectorAll('input');
const datePlace = document.querySelector('.date');

export {
  navlist, section, bookShelf, newBook, book, datePlace,
};
